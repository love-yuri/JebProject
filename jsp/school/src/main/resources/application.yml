spring:
  datasource:
    url: jdbc:mysql://localhost:3306/school
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: yuri
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    static-path-pattern: /static/**
  web:
    resources:
      static-locations: ./resources/upload
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB
server:
  port: 8848
